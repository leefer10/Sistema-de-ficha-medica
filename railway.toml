# Railway configuration for OCR Medical Records System

[build]
builder = "dockerfile"
dockerfile = "Dockerfile"

[deploy]
startCommand = "python -m uvicorn app.main:app --host 0.0.0.0 --port 8000"
restartPolicyMaxRetries = 5
